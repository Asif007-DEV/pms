<%-include('header') %>
    <div class="container">
        <div class="row">
          <div id="signupbox" style="margin-top:50px" class="mainbox col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2">
            <div class="panel panel-info">
              <div class="panel-heading">
                <div class="panel-title"><%= title %></div>
                <div style="float: right; font-size: 85%; position: relative; top: -10px;" ></div>
              </div>
              <div class="panel-body">
                <form action="/passwordList/edit/" id="addNewCateoryForm" class="form-horizontal" role="form" method="post">
                    <input type="hidden" name="Passid" value="<%= records._id %>">
                  <div class="form-group">
                    <label for="username" class="col-md-3 control-label">Password Category:</label>
                    <div class="col-md-9">
                        <select name="editpasswordCategory">
                            <% if(category.length >0){%>
                                <% category.forEach(function(row){%>
                                    <option value="<%= row.password_category %>" <% if(row.password_category== records.password_category) {%> selected <%} %> ><%= row.password_category %></option>
                                <%}) %>    
                            <%} %>
                        </select>
                    </div>
                  </div>

                  <div class="form-group">
                    <label for="username" class="col-md-3 control-label">Project name:</label>
                    <div class="col-md-9">
                      <input type="text" class="form-control" name="editProjectName" value="<%= records.project_name %>">
                    </div>
                  </div>

                  <div class="form-group">
                    <label for="username" class="col-md-3 control-label">Password Details:</label>
                    <div class="col-md-9">
                      <textarea type="text" rows="5" id="editor" class="form-control" name="editPasswordDetails"><%= records.add_password_details %></textarea>
                    </div>
                  </div>
    
                  <div class="form-group">
                    <label class="col-md-3 control-label"></label>
                    <div class="col-md-9">
                      <input type="submit" type="button" class="btn btn-info" value="Edit Password">
                    </div>
                  </div>
                </form>
              </div>
            </div>
          </div>
        </div>
      </div>
      <script>
        CKEDITOR.replace('editor')
      </script>
  </body>
</html>
